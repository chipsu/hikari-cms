%if is_traversable($result)
#%if $result is traversable
    #$attr=[class="poo" @poo="oop"]
    header $attr
        h1
            #"Hello"
    ul#poo class="list" &menu-id="poo" @rand=(rand() % 10) @prefix=[a=2 b=3]
        %for $key $value in $result
            li @id=$value.id()
                %encode($value)
%else
    div.empty @poo="oop"
        #"Poo"
