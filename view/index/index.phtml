<?php

echo $this->html->open('pre');
	var_dump($page);
echo $this->html->close('pre');

echo $this->html->open('pre');
	var_dump($pages);
echo $this->html->close('pre');

foreach($pages as $page) {
	echo $this->html->tag('hr');
	echo $this->html->a($this->url('page', ['action' => 'read', 'id' => (string)$page->id()]), [], $page->id());
	echo $this->html->a($this->url('page', ['action' => 'remove', 'id' => (string)$page->id()]), ['class' => 'button'], 'Delete');
}

#$page->delete();